#:schema https://mise.jdx.dev/schema/mise.toml.json

# When updating versions, also update .ruby-version and .node-version
# for compatibility with rbenv, nodenv, and CI systems.
[tools]
ruby = "3.4.4"
node = "20.11.1"
